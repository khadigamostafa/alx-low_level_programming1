#include <stdio.h>
/**
 * main - the main function describes the main common in all c program
 *
 *Return: it is  always 0
 */
int main(void)
{
	int num = 0;

	while (num <= 9)
	{
		Printf(“ % d ”, num);
		num++;
	}
	printf(“\n”);
	return (0);
}
